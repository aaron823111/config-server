spring:
  redis:
    host: 192.168.99.100
    port: 6379
  session:
    #REDIS使用redis作为会话存储、NONE则使用中间件的会话，用户登录信息使用EHcache存储,默认为EHcache存储
    storeType: REDIS

px:
  datasource:
    dataSource:
      type: com.alibaba.druid.pool.DruidDataSource 
      driverClassName: com.mysql.jdbc.Driver
      url: jdbc:mysql://10.148.69.148:3306/pi6000
      username: root
      password: root
      initialSize: 5
      maxActive: 20
      
eureka:
  client:
    enabled: true
    serviceUrl:
      defaultZone: http://discovery:8761/eureka/
  instance:
    preferIpAddress: true
